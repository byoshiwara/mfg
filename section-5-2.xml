<?xml version="1.0" encoding="UTF-8" ?>
<!-- <mathbook><book> -->

<section xml:id="Logarithmic-Functions"   xmlns:xi="http://www.w3.org/2001/XInclude">
<title>Logarithmic Functions</title>

<subsection><title>Inverse of the Exponential Function</title>
<p>
	Inverse functions are really a generalization of inverse operations. For example, raising to the <m>n</m>th power and taking <m>n</m>th roots are inverse operations. In fact, we use the following rule to define cube roots:
	<me>b = \sqrt[3]{a} ~\text{ if and only if }~ a = b^3</me>
	Compare this rule to the definition of inverse functions from <xref ref="Inverse-Functions" autoname="yes"/>. In this case, if <m>f(x) = x^3</m> and <m>g(x) = \sqrt[3]{x}</m>, we see that
	<me>b = g(a) ~\text{ if and only if }~ a = f (b)</me>
	We have shown that the two functions <m>f(x) = x^3</m> and <m>g(x) = \sqrt[3]{x}</m> are inverse functions.
</p>
<p>
	In <xref ref="chap4" autoname="yes"/>, we saw that a similar rule relates the operations of raising a base <m>b</m> to a power and taking a base <m>b</m> logarithm, because they are inverse operations.
</p>

<assemblage><title>Conversion Formulas for Logarithms</title>
	<p>
		For any base <m>b \gt 0, b\ne 1</m>,
		<me>y = \log_{b}{x} ~\text{ if and only if }~ x = b^y</me>
	</p>
</assemblage>

<p>
	We can now define the <term>logarithmic function</term>, <m>g(x) = \log_{b}{x}</m>, that takes the log base <m>b</m> of its input values. The conversion formulas tell us that the log function, <m>g(x) = \log_{b}{x}</m>, is the inverse of the exponential function, <m>f(x) = b^x</m>.
</p>

<assemblage><title>Logarithmic Function</title>
	<p>
		The <term index="yes">logarithmic function</term> base <m>b</m>, <m>g(x) = \log_b x</m>, is the inverse of the exponential function of the same base, <m>f(x) = b^x</m>.
	</p>
</assemblage>

<p>
	For example, the function <m>g(x) = \log_2 x</m> is the inverse of <m>f(x) = 2^x</m>. Each function undoes the effect of the other. So, if we start with <m>x = 3</m>, apply <m>f</m>, and then apply <m>g</m> to the result, we return to the original number, 3.
	<me>
		x=\alert{3}\mathrel{\mathop{\rightarrow}^{\text{Apply the}}_{\text{exponential function}}}
		 f(\alert{3})=2^\alert{3}=8		
		\mathrel{\mathop{\rightarrow}^{\text{Apply the}}_{\text{log function}}}
		 g(8)=\log_2 8 = 
		\mathrel{\mathop{\alert{3}}^{\text{Original}}_{\text{number}}}
	</me>
	We can write both calculations together as
	<me>\log_{2}\left(2^\alert{3}\right) = \alert{3}</me>
	A similar equation holds for any value of <m>x</m> and for any base <m>b \gt 0</m>. In other words, applying first the exponential function and then the log function returns the original input value, so that
	<me>\log_{b}{b^x} = x</me>
</p>

<example xml:id="example-log-of-exp">
<p>
	Simplify each expression.
	<ol label="*a*">
		<li><m>\log_{4}{4^6}</m></li>
		<li><m>\log_{8}{8^{2a+3}}</m></li>
	</ol>
</p>
<solution>
	<ol label="*a*">
		<li>
			In this expression, we start with <m>6</m>, apply the exponential function with base <m>4</m>, and then take a logarithm base <m>4</m>. Because the logarithm is the inverse of the exponential function, we return to the original number, <m>6</m>.
			<me>
				x=6\mathrel{\mathop{\rightarrow}^{\text{Apply the}}_{\text{exponential function}}}
				 4^6		
				\mathrel{\mathop{\rightarrow}^{\text{Apply the}}_{\text{log function}}}
				\log_4 {4^6} = 
				\mathrel{\mathop{6}^{\text{Original}}_{\text{number}}}
			</me>
		</li>
		<li>
			The input of the exponential function is the expression <m>2a + 3</m>. Because the bases of the log and the exponential function are both <m>8</m>, they are inverse functions, and applying them in succession returns us to the original input. Thus, <m>\log_{8}{8^{2a+3}} = 2a + 3</m>.
		</li>
	</ol>
</solution></example>

<exercise xml:id="exercise-log-of-exponential"><statement>
<p>
	Simplify each expression.
	<ol label="*a*">
		<li><m>\log_{10}{10^6}</m></li>
		<li><m>\log_{w}{w^{x+1}}</m>, for <m>w \gt 0, w\ne 1</m></li>
	</ol>
</p>
</statement></exercise>

<p>
	We can also apply the two functions in the opposite order. For example,
	<me>2^{\log_2 \alert{8} }= \alert{8}</me>
	To see that this equation is true, we simplify the exponent first. We start with <m>8</m>, and apply the log base <m>2</m> function. Because <m>\log_2 8 = 3</m>, we have
	<me>
		\alert{8}\mathrel{\mathop{\rightarrow}^{\text{Apply the}}_{\text{log function}}}
		 \log_{2}{\alert{8}}=3		
		\mathrel{\mathop{\rightarrow}^{\text{Apply the}}_{\text{exponential function}}}
		 2^{\log_2 \alert{8} }= 
		\mathrel{\mathop{\alert{8}}^{\text{Original}}_{\text{number}}}
	</me>
	Of course, a similar equation holds for any positive value of <m>x</m> and any base <m>b \gt 0, b\ne 1</m>:
	<me>b^{\log_b x} = x</me>
</p>

<example xml:id="example-exp-of-log">
	<p>
		Simplify each expression.
		<ol label="*a*">
			<li><m>10^{\log_{10}{1000}}</m></li>
			<li><m>Q^{\log_{Q}{25}}\text{, for } Q \gt 0, Q\ne 1</m></li>
		</ol>
	</p>
<solution>
	<ol label="*a*">
		<li>In this expression, we start with <m>1000</m>, take the logarithm base <m>10</m>, and then apply the exponential function base <m>10</m> to the result. We return to the original input, so <m>10^{\log_{10}{1000}} = 1000</m>.</li>
		<li>The log function, <m>\log_{Q}{x}</m>, and the exponential function, <m>Q^x</m>, are inverse functions, so <m>Q^{\log_{Q}{25}} = 25</m>.</li>
	</ol>
</solution>
</example>

<exercise xml:id="exercise-exp-of-log"><statement>
	<p>
		Simplify each expression.
		<ol label="*a*">
			<li><m>4^{\log_4 {64}}</m></li>
			<li><m>2^{\log_{2}{(x^2+1)}}</m></li>
		</ol>
	</p>
</statement></exercise>

<p>
	We summarize these relationships as follows.
</p>

<assemblage><title>Exponential and Logarithmic Functions</title>
	<p>
		Because <m>f(x) = b^x</m> and <m>g(x) = \log_{b}{x}</m> are inverse functions for <m>b \gt 0, b\ne 1</m>,
		<me>
			\log_{b}{b^x} = x\text{, for all }x ~~\text{ and }~~b^{\log_b x} = x\text{, for }x \gt 0
		</me>
	</p>
</assemblage>

</subsection>



<subsection><title>Graphs of Logarithmic Functions</title>
<p>
	We can obtain a table of values for <m>g(x) = \log_2 x</m> by making a table for <m>f(x) = 2^x</m> and then interchanging the columns, as shown in <xref ref="table-base-2-exp">Tables</xref> and <xref ref="table-base-2-log" autoname="no"/>. You can see that the graphs of <m>f(x) = 2^x</m> and <m>g(x) = \log_2 x</m>, shown in <xref ref="fig-log-and-exp-on-same-grid" autoname="yes"/>, are symmetric about the line <m>y = x</m>.
	<sidebyside>
	<table xml:id="table-base-2-exp" width="30%" valign="middle" ><tabular top="major" halign="center" right="minor" left="minor" bottom="minor">
        <row bottom="minor">
            <cell><m>x</m></cell>
            <cell><m>f(x)=2^x</m></cell>
        </row>
        <row>
            <cell><m>-2</m></cell>
            <cell><m>\frac{1}{4}</m></cell>
        </row>
        <row>
            <cell><m>-1</m></cell>
            <cell><m>\frac{1}{2}</m></cell>
        </row>
        <row>
            <cell><m>0</m></cell>
            <cell><m>1</m></cell>
        </row>
        <row>
            <cell><m>1</m></cell>
            <cell><m>2</m></cell>
        </row>
        <row>
            <cell><m>2</m></cell>
            <cell><m>4</m></cell>
        </row>
    </tabular><caption></caption></table>

	<table xml:id="table-base-2-log" width="30%" valign="middle" ><tabular top="major" halign="center" right="minor" left="minor" bottom="minor">
        <row bottom="minor">
            <cell><m>x</m></cell>
            <cell><m>g(x)=\log_{2}{x}</m></cell>
        </row>
        <row>
            <cell><m>\frac{1}{4}</m></cell>
            <cell><m>-2</m></cell>
        </row>
        <row>
            <cell><m>\frac{1}{2}</m></cell>
            <cell><m>-1</m></cell>
        </row>
        <row>
            <cell><m>1</m></cell>
            <cell><m>0</m></cell>
        </row>
        <row>
            <cell><m>2</m></cell>
            <cell><m>1</m></cell>
        </row>
        <row>
            <cell><m>4</m></cell>
            <cell><m>2</m></cell>
        </row>
   </tabular><caption></caption></table>
	<figure valign="middle"  width="40%" xml:id="fig-log-and-exp-on-same-grid"><image source="images/fig-log-and-exp-on-same-grid" alt="graph of log and exponential base 2"/><caption></caption></figure>
</sidebyside>
</p>

<example xml:id="example-log10-and-exp">
	<p>
		Graph the function <m>f(x)=10^x</m> and its inverse <m>g(x)=\log_{10}{x}</m> on the same axes.
	</p>
<solution>
<p>
	Make a table of values for the function <m>f(x)=10^x</m>. A table of values for the inverse function, <m>g(x) = \log_{10}{x}</m>, can be obtained by interchanging the components of each ordered pairs in the table for <m>f</m>.
	<sidebyside>
	<table width="23%" valign="middle" ><tabular top="major" halign="center" right="minor" left="minor" bottom="minor">
        <row bottom="minor">
            <cell><m>x</m></cell>
            <cell><m>f(x)</m></cell>
        </row>
        <row>
            <cell><m>-2</m></cell>
            <cell><m>0.01</m></cell>
        </row>
        <row>
            <cell><m>-1</m></cell>
            <cell><m>0.1</m></cell>
        </row>
        <row>
            <cell><m>0</m></cell>
            <cell><m>1</m></cell>
        </row>
        <row>
            <cell><m>1</m></cell>
            <cell><m>10</m></cell>
        </row>
        <row>
            <cell><m>2</m></cell>
            <cell><m>100</m></cell>
        </row>
    </tabular></table>

	<table width="23%" valign="middle" ><tabular top="major" halign="center" right="minor" left="minor" bottom="minor">
        <row bottom="minor">
            <cell><m>x</m></cell>
            <cell><m>g(x)</m></cell>
        </row>
        <row>
            <cell><m>0.01</m></cell>
            <cell><m>-2</m></cell>
        </row>
        <row>
            <cell><m>0.1</m></cell>
            <cell><m>-1</m></cell>
        </row>
        <row>
            <cell><m>1</m></cell>
            <cell><m>0</m></cell>
        </row>
        <row>
            <cell><m>10</m></cell>
            <cell><m>1</m></cell>
        </row>
        <row>
            <cell><m>100</m></cell>
            <cell><m>2</m></cell>
        </row>
    </tabular></table>
	<figure valign="middle"  width="54%" xml:id="fig-log10-and-exp-on-same-grid"><image source="images/fig-log10-and-exp-on-same-grid" alt="graph of common log and exponential"/><caption></caption></figure>
	</sidebyside>
		Plot each set of points and connect them with smooth curves to obtain the graphs shown in <xref ref="fig-log10-and-exp-on-same-grid" autoname="yes"/>.
	</p>
</solution>
</example>

<exercise xml:id="exercise-log-base-4"><statement>
	<p>
		Make a table of values and graph the function <m>h(x) = \log_4 x</m>.
	</p>
</statement></exercise>

<p>
	While an exponential growth function increases very rapidly for positive values, its inverse, the logarithmic function, grows extremely slowly, as you can see in <xref ref="fig-log10-and-exp-on-same-grid" autoname="yes"/>. In addition, the logarithmic function <m>y = \log_b x</m> for any base <m>b \gt 0, b \ne 1</m>, has the following properties.
</p>

<assemblage><title>Logarithmic Functions <m>y = \log_b x</m></title>
	<p>
		<ol>
			<li>
				Domain: all positive real numbers
			</li>
			<li>
				Range: all real numbers
			</li>
			<li>
				<m>x</m>-intercept: <m>(1, 0)</m>
			</li>
			<li>
				<m>y</m>-intercept: none
			</li>
			<li>
				Vertical asymptote at <m>x = 0</m>
			</li>
			<li>
				The graphs of <m>y = \log_b x</m> and <m>y = b^x</m> are symmetric about the line <m>y = x</m>.
			</li>
		</ol>
	</p>
</assemblage>

<example xml:id="example-invert-translated-exponential">
	<ol label="*a*">
		<li>Find the inverse of the function <m>f(x) = 2^{x−3} − 4</m>.</li>
		<li>Graph <m>f</m> and <m>f^{−1}</m> on the same grid.</li>
		<li>State the domain and range of <m>f</m> and of <m>f^{−1}</m>.</li>
	</ol>
<solution>
	<ol label="*a*">
		<li>
			Write the function as <m>y = 2^{x−3} − 4</m>, and solve for <m>x</m> in terms of <m>y</m>. First, isolate the power:
			\begin{align*}
			y + 4 \amp= 2^{x−3}\amp\amp\text{Take logs base 2.}\\
			\log_{2}{(y + 4)} \amp= \log_{2}{2x−3}  = x − 3\\
			x \amp = 3 + \log_{2}{(y + 4)}
			\end{align*}
			The inverse function is <m>f^{−1}(y) = 3 + \log_{2}{(y + 4)}</m>. However, to graph both <m>f</m> and <m>f^{−1}</m> on the same grid, we write the inverse function as <m>f^{−1}(x) = 3 + \log_{2}{(x + 4)}</m>.
		</li>
		<li>
			To graph <m>f</m>, we translate the graph of <m>y = 2^x</m> by <m>3</m> units to the right and <m>4</m> units down. The graph of <m>f^{−1}</m> looks like the graph of <m>y = \log_2 x</m> shown in <xref ref="fig-log-and-exp-on-same-grid" autoname="yes"/>, but shifted <m>4</m> units to the left and <m>3</m> units up. The graphs are shown in <xref ref="fig-invert-translated-exponential" autoname="yes"/>, along with the line <m>y = x</m>.
			<figure xml:id="fig-invert-translated-exponential"><image source="images/fig-invert-translated-exponential" alt="graph of translated exponential and inverse" width="75%"/><caption></caption></figure>
		</li>
		<li>
			The function <m>f</m> is a translation of an exponential function, and its domain consists of all real numbers. Because the graph is shifted <m>4</m> units down, the range of <m>f</m> is <m>(−4,\infty)</m>. Because the log of a negative number or zero is undefined, for <m>f^{−1}(x) = 3 + \log_{2}{(x + 4)}</m>, we must have <m>x + 4 \gt 0</m>, or <m>x \gt −4</m>. We can verify on the graph that the range of <m>f^{−1}</m> includes all real numbers. Thus,
			\begin{align*}
			\amp\text{Domain}(f) = \text{all real numbers} = \text{Range}(f^{−1})\\
			\amp\text{Range}(f) = (−4,\infty) = \text{Domain} (f^{−1})
			\end{align*}
		</li>
	</ol>
</solution>
</example>

<exercise xml:id="exercise-invert-translated-log"><statement>
	<ol label="*a*">
		<li>Find the inverse function for <m>f(x) = 2 \log(x + 1)</m>.</li>
		<li>Graph <m>f</m> and <m>f^{−1}</m> in the window
			\begin{align}
	        \text{Xmin} \amp = −6 \amp\amp \text{Xmax} = 6\\
	        \text{Ymin} \amp = −4 \amp\amp \text{Ymax} = 4
	        \end{align}</li>
	    <li>State the domain and range of <m>f</m> and <m>f^{−1}</m>.</li>
	</ol>
</statement></exercise>

</subsection>



<subsection><title>Evaluating Logarithmic Functions</title>
<p>
	We can use the <c>LOG</c> key on a calculator to evaluate the function <m>f(x) = \log_{10}{x}</m>.
</p>

<example xml:id="example-GC-evaluate-logs">
<p>
	Let <m>f(x) = \log_{10}{x}</m>. Evaluate the following expressions.
	<ol label="*a*">
		<li><m>f(35)</m></li>
		<li><m>f(−8)</m></li>
		<li><m>2 f(16) + 1</m></li>
	</ol>
</p>
<solution>
	<ol label="*a*">
		<li><m>f (35) = \log_{10}{35}\approx 1.544</m></li>
		<li>Because <m>−8</m> is not in the domain of <m>f</m>, <m>f(−8)</m>, or <m>\log_{10}{(−8)}</m>, is undefined.</li>
		<li><m>2 f(16) + 1 = 2(\log_{10}{16}) + 1\approx2(1.204) + 1 = 3.408</m></li>
	</ol>

</solution></example>

<exercise xml:id="exercise-tumor-doubling-time"><statement>
<p>
	The formula <m>T = \frac{\log 2 \cdot t_i}{3 log(D_f /D_0)}</m> is used by X-ray technicians to calculate the doubling time of a malignant tumor. <m>D_0</m> is the diameter of the tumor when first detected, <m>D_f</m> is its diameter at the next reading, and <m>t_i</m> is the time interval between readings, in days. Calculate the doubling time of a tumor if its diameter when first detected was <m>1</m> cm, and <m>1.05</m> cm <m>7</m> days later.
</p>
</statement></exercise>

</subsection>



<subsection><title>Logarithmic Equations</title>
<p>
	A <term index="yes">logarithmic equation</term> is one in which the variable appears inside of a logarithm. For example,
	<me>\log_4 x = 3</me>
	is a log equation. To solve a log equation, remember that logarithms and exponentials with the same base are inverse functions. Therefore, 
	<me>y = \log_b x ~~ \text{ if and only if } ~~ x = b^y</me> 
	Thus, we can rewrite a logarithmic equation in exponential form.
</p>

<example xml:id="example-log-equations">
<p>
	Solve for <m>x</m>.
	<ol label="*a*">
		<li><m>2(\log_3 x) − 1 = 4</m></li>
		<li><m>\log_{10}{(2x + 100)} = 3</m></li>
	</ol>
</p>
<solution>
	<ol label="*a*">
		<li>
			Isolate the logarithm, then rewrite in exponential form:
			\begin{align*}
			2(\log_3 x) \amp = 5\amp\amp\text{Divide both sides by 5.}\\
			\log_3 x \amp = \frac{5}{2}\amp\amp\text{Convert to exponential form.}\\
			x \amp = 3^{5/2}
			\end{align*}
		</li>
		<li>
			First, convert the equation to exponential
			<me>2x + 100 = 10^3 = 1000</me>
			Solve for <m>x</m> to find <m>2x = 900</m>, or <m>x = 450</m>.
		</li>
	</ol>
</solution>
</example>

<exercise xml:id="exercise-log-equation"><statement>
<p>
	Solve for the unknown value in each equation.
	<ol label="*a*">
		<li><m>
			\log_b 2 = \frac{1}{2}</m>
		</li>
		<li><m>
			\log_{3}{(2x − 1)} = 4</m>
		</li>
	</ol>
</p>
</statement></exercise>

<example xml:id="example-common-log-equation">
	<p>
		If <m>f(x) = \log_{10}{x}</m>, find <m>x</m> so that <m>f(x)=−3.2</m>.
	</p>
<solution>
	<p>
		We must solve the equation <m>\log_{10}{x} = −3.2</m>. Rewriting the equation in exponential form yields
		<me>x = 10^{−3.2}\approx 0.00063</me>		
	</p>
</solution>
</example>

<remark><title><image source="images/icon-GC.jpg" alt="Graphing Calculator" width="8%"  />Evaluating <m>10^x</m></title>
<p>
	In <xref ref="example-common-log-equation" autoname="yes"/>, the expression <m>10^{−3.2}</m> can be evaluated in two different ways with a calculator. We can use the <c>^</c> key and press
</p>
<p>
	<m>10</m> <c>^</c> <c>(−)</c> <m>3.2</m> <c>ENTER</c>
</p>
<p>
	which gives <c>6.30957344 E −4</c>, or approximately <m>0.00063</m>. Alternatively, because <m>10^x</m> is the inverse function for <m>\log x</m>, we can press
</p>
<p>
	<c>2nd</c> <c>LOG</c> <c>(−)</c> <m>3.2</m> <c>ENTER</c>
</p>
<p>
	which gives the same answer as before.
</p>
</remark>

<exercise xml:id="exercise-common-log-equation"><statement>
	<p>
		Imagine the graph of <m>f(x) = \log_{10}{x}</m>. How far must you travel along the <m>x</m>-axis until the <m>y</m>-coordinate reaches a height of <m>5.25</m>?
	</p>
</statement></exercise>

</subsection>



<subsection><title>Using the Properties of Logarithms</title>
<p>
	The properties of logarithms are useful in solving both exponential and logarithmic equations. To solve logarithmic equations, we first combine any expressions involving logs into a single logarithm.
</p>

<example xml:id="example-solve-log-equation">
	<p>
		Solve <m>\log_{10}{(x + 1)} + \log_{10}{(x − 2)} = 1</m>.
	</p>
<solution>
<p>
	Use Property (1) of logarithms to rewrite the left-hand side as a single logarithm:
	<me>\log_{10}{(x + 1)(x − 2)} = 1</me>
	Once the left-hand side is expressed as a single logarithm, we can rewrite the equation in exponential form as
	<me>(x + 1)(x − 2) = 10^1</me>
	from which
	\begin{align*}
	x^2 − x − 2 \amp = 10\amp\amp\text{Subtract 10 from both sides.}\\
	x^2 − x − 12 \amp = 0\amp\amp\text{Factor the left side.}\\
	(x − 4)(x + 3) \amp= 0\amp\amp\text{Apply the zero-factor principle.}
	\end{align*}
	Thus, <m>x = 4</m> or <m>x = −3</m>. The number <m>−3</m> is not a solution of the original equation, because neither <m>\log_{10}{(x + 1)}</m> nor <m>\log_{10}{(x − 2)}</m> is defined for <m>x = −3</m>. The solution of the original equation is <m>4</m>.
</p></solution></example>

<p>
	In <xref ref="example-solve-log-equation" autoname="yes"/>, the apparent solution <m>x = −3</m> is called <term index="yes">extraneous</term> because it does not solve the original equation. We should always check for extraneous solutions when solving log equations. The following steps give a rough outline for solving log equations.
</p>

<assemblage><title>Steps for Solving Logarithmic Equations</title>
<p>
	<ol>
		<li>
			Use the properties of logarithms to combine all logs into one log.
		</li>
		<li>
			Isolate the log on one side of the equation.
		</li>
		<li>
			Convert the equation to exponential form.
		</li>
		<li>
			Solve for the variable.
		</li>
		<li>
			Check for extraneous solutions.
		</li>
	</ol>
</p>
</assemblage>

<exercise xml:id="exercise-solve-log-equation"><statement>
	<p>
		Solve <m>\log_2 x + \log_{2}{(x − 2)} = 3</m>.
	</p>
	<hint>
		<p>Rewrite the left side as a single logarithm.</p>
		<p>Rewrite the equation in exponential form.</p>
		<p>Solve for <m>x</m>.</p>
		<p>Check for extraneous solutions.</p>
	</hint>
</statement></exercise>

</subsection>


</section> 
<!-- </book>  </mathbook> -->